import styled from '@emotion/styled';
import theme from '../Themes/theme';

const Wrapper = styled.div`
  height: 100%;
  width: 90%;
  max-width: ${theme.commons.style.maxWidthWrappers}px;
  margin: 0 auto;
`;

export default Wrapper;
