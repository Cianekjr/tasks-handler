import { css } from '@emotion/core';

const Normallize = css`
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  body {
    font-size: 10px;
    font-family: Roboto, sans-serif;
    overflow: hidden;
  }
`;

export default Normallize;
